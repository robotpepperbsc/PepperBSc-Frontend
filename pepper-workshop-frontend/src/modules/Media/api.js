export function fetchMedia() {
  // return fetch("/media").then(response =>
  //   response.json().then(notes => notes.reverse())
  // );

  return [
    {
      name: "media1"
    },
    {
      name: "media2"
    },
    {
      name: "media3"
    }
  ];
}
