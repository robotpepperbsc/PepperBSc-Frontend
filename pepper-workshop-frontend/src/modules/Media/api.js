export function fetchMedia() {
  // return fetch("/notes").then(response =>
  //   response.json().then(notes => notes.reverse())
  // );

  return [
    {
      name: "media1"
    },
    {
      name: "media2"
    },
    {
      name: "media3"
    }
  ];
}
